<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- The metric/imperial chips differ only by id, label, and description. -->
    <style name="Widget.HeightMark.UnitChip" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">@dimen/min_touch_target</item>
        <item name="android:minWidth">@dimen/unit_chip_min_width</item>
    </style>

</resources>
